# Código server-only preservado (TanStack Start)

Estes arquivos **não entram no build SPA**. Foram retirados do grafo do Vite/TanStack Router para que `npm run build` gere `dist/` estático.

Não apague até a fase PHP. Ver `docs/SERVER-FUNCTIONS-MIGRATION.md`.
